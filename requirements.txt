# Runtime dependencies — the install_requires set from setup.py.
#
# Dev tooling lives in requirements-dev.txt. Pinned hashed lockfiles
# live in requirements.lock and requirements-dev.lock; production
# installs and CI should use those with `pip install --require-hashes`.
cryptography>=49.0.0
dnspython>=2.8.0
reedsolo>=1.7.0
pyyaml>=6.0.3
argon2-cffi>=25.1.0
zfec>=1.6.0.0

# DNS publisher backends
requests>=2.34.2
boto3>=1.43.29

# Async support
asyncio-throttle>=1.0.2
