# Development-only dependencies (tests, linters, type-checkers, fuzzing,
# supply-chain tools). Runtime deps are in requirements.txt; the pinned
# hashed lockfile for dev tooling is requirements-dev.lock.
-r requirements.txt

pytest>=9.0.3
pytest-asyncio>=1.3.0
pytest-cov>=7.1.0
pytest-timeout>=2.4.0
black>=23.0.0
mypy>=1.0.0
pylint>=2.17.0

# Property-based / fuzz testing for wire parsers.
hypothesis>=6.100.0

# Supply-chain: generate hashed lockfiles and audit for CVEs.
pip-tools>=7.5.3
pip-audit>=2.7.0
